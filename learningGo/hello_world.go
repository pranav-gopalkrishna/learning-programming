package main

import "fmt"

func main() {
	fmt.Print("Hello World")
}

// NOTE: `` works the same as "", so fmt.Print(`Hello World`) is also valid (single quotes '' for strings is not allowed, though).
